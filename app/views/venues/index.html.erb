<div id="latest_venue">
	<p>
		<strong>Latest Available Venues:</strong>
			<% @venues.all.last(10).each do |v|%>
			<ul>
				<li><strong><%= v.venue_title %></strong><br /><%= v.address %><br /><br /><%= image_tag v.avatar :thumb %> <br /> <%= link_to "View Details >", venue_path(v.id) %></li>
			</ul>
			<% end %>
	</p>
</div>